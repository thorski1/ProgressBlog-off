{
  "name": "progress-blog",
  "version": "1.0.0",
  "description": "Blog tracking my daily progress",
  "author": "Sam Thoyre",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "@nuxtjs/axios": "^5.3.6",
    "nuxt": "^2.15.8",
    "storyblok-nuxt": "^1.0.1"
  },
  "devDependencies": {
    "nodemon": "^1.18.9",
    "push-dir": "^0.4.1"
  }
}
